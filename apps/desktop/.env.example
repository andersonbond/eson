# eson-agent base URL (browser dev, production build).
# Under `tauri dev`, the app uses same-origin `/eson-api` (Vite proxy → this port) so WKWebView can reach the agent.
VITE_ESON_AGENT_URL=http://127.0.0.1:8787
